const should = require('should')
const mongoose = require('mongoose');
const Produkt = require('../models/product');

describe('Test suite', () => {

    it('Find ID', () => {
        
        

    })

})